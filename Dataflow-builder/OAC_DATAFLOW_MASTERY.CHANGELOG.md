# OAC DATA FLOW MASTERY — CHANGELOG (tách khỏi golden, KHÔNG nạp mỗi phiên)

## 18. CHANGELOG
- 2026-06-11 (đa session): Project có MCP server riêng `chrome-dataflow` (profile-dataflow, `.mcp.json`) + CLAUDE.md luật session; supersede gỡ-kẹt cũ → kill chrome THEO PROFILE, cấm kill toàn cục (§1, bảng walls §12 vẫn ghi restart-server là bước cuối).
- 2026-06-11: Build thật **KGR_DF_ACTUAL_AOP_MONTHLY_v1** (báo cáo ước tính tháng từ `(KGR) DTF_ACTUAL_AOP_EXPENSE`) → rút nhiều finding (§7, §10). Thêm: pattern pivot kỳ→tháng (CASE+SUM) + clone def NGUỒN + AOP=MAX (§7.18); executePreview giới hạn ~31 cột (cắt passthrough, preview-only) + đọc nguồn chập chờn (Run materialized ổn định) + WAF `_abck` 401 (§10); POST /run qua fetch CHẠY (supersede "classifier chặn /run") (§10); sửa path file ở §17 (đã move sang C:\Project\KGR-OAC-Agents\Dataflow-builder).
- ⚠️ Login: lần này dùng nhầm password từ MEMORY cũ (`<OAC_PW — doc C:/Project/KGR-OAC-Agents/.secrets/oac.env (gitignored)>`) → "Invalid username or password"; §0 file ghi `<OAC_PW — doc C:/Project/KGR-OAC-Agents/.secrets/oac.env (gitignored)>`. **Bài học: tin §0 của file này, KHÔNG tin password trong memory cũ** (memory có thể lỗi thời). User tự login tay khi creds sai.
- 2026-06-11 (tiếp): User báo v1 (REST-built) **KHÔNG mở/Run được trong editor**. Debug: editor mở nhưng canvas TRỐNG + console `aggrule is not a function`. Fix: clone **KGR_DF_ACTUAL_AOP_MONTHLY_v2** với metadata editor đầy đủ (aggrule + srcexpression + step display + autoLayout, clone schema từ flow nguồn `DF_ACTUAL_AOP_EXPENSE`) → v2 MỞ/SỬA/RUN-UI được, số khớp v1 (921.35/28.10/21.31, 312 dòng). Thêm §11.5 (def editor-openable) + sửa §10 RUN. Bài học: def REST tối-giản chạy /run đúng số nhưng KHÔNG editor-friendly — luôn dựng đủ metadata §11.5.
- 2026-07-16: Build **KGR_DF_DAILY_NGANH_LITE_v1** (feature BC01 P2b — daily as-of cumulative theo NGÀNH × Kênh/Chuỗi). **Pattern LITE-CLONE của cỗ-máy as-of** (thay vì luồn dim mới xuyên 87-step `DF_DAILY_SC_CHAIN_ASOF`): GET def flow production đầy-đủ → **giữ CHỈ prefix + lõi as-of, cắt ~70-step đuôi** (AOP-join / universe-zerofill / cost-rate KD / dedup — các step này keyed KHÔNG có dim mới nên thêm dim = **fan-out trap**). Lõi as-of tối-giản/1 nhánh = `InputDataset → SelectColumns → AddColumns(SUB_ID) → Branch → [RenameColumns TRANDATE→AsOfDate → SelectColumns(AsOfDate,PERIODNAME) → GroupBy distinct → | SelectColumns fact | → Join rightouterjoin `PERIODNAME=PERIODNAME & AsOfDate>=TRANDATE` → GroupBy cumulative]`. Thêm dim mới = thêm cột vào **SelectColumns-fact + groupByColumns của GroupBy-cumulative** (2 chỗ). Non-equi join có `PERIODNAME=PERIODNAME` ⇒ cumulative RESET theo kỳ ⇒ CKKM/DT tự căn-đúng-kỳ, và tại as-of cuối kỳ = tổng line-level (ZERO fan-out). Verify LIVE: Σ DT ngành T6 tại AsOfDate=06-30 khớp fact-hub tới từng chữ số (Water 221.93/Home 88.52/Cold 39.67/Sanitary 2.76 = 352.88 tỷ), CKKM khớp SUM(CP CKKM) T6 (46.31/16.33/3.98/0.41 tỷ), đường cong cumulative 30 điểm đơn-điệu. Nguồn: `(KGR) DTF_CALC_INVOICE_MEMO_#` (owner anhdk) sạch ở grain ngành — DS_TT=sum("Doanh số thực tế"), CP_CKKM=sum("CP CKKM"). Thêm §10 gotcha GUID-namespace (nguyên nhân "canvas trống" lần này KHÔNG phải §11.5 mà là SAI id khi mở editor).
